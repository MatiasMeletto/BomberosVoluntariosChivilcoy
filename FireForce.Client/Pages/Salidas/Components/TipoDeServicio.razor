@using FireForce.Shared.Enums;
@using FireForce.Shared.Enums.Salidas;
@using FireForce.Client.Data.ViewModels;
@using FireForce.Client.Data.ViewModels.Incendios;
@using FireForce.Client.Data.ViewModels.MaterialesPeligrosos;
@using FireForce.Client.Data.ViewModels.Accidente;
@using FireForce.Client.Data.ViewModels.FactorClimatico;
@using FireForce.Client.Data.ViewModels.Servicios;

<!-- Ejemplo de estructura estilizada para Tipo de Servicio con AntDesign Blazor -->
<style>
    .ts-container {
        padding: 24px;
        background: #fafafa;
        border-radius: 16px;
        border: 1px solid #e5e5e5;
    }

    .ts-section-title {
    font-size: 17px !important;
    font-weight: 600 !important;
    margin-bottom: 16px !important;
    padding-bottom: 6px !important;
    border-bottom: 2px solid #d0d0d0 !important;
    color: #333 !important;
}

.ant-form-item {
    margin-bottom: 18px !important;
}

.ant-input,
.ant-select-selector,
.ant-input-number {
    border-radius: 14px !important;
}

.ant-input:hover,
.ant-select-selector:hover,
.ant-input-number:hover {
    border-color: #1677ff;
}

.ts-check-item {
    box-shadow: inset 0 0 0 1px #e0e0e0;
}

.ts-block {
    transition: box-shadow .2s ease, transform .2s ease, opacity .15s ease;
}


    .ts-section-title {
        font-size: 18px;
        font-weight: 600;
        margin-bottom: 12px;
        padding-bottom: 8px;
        border-bottom: 2px solid #d9d9d9;
    }

    .ts-block {
        background: white;
        padding: 20px;
        border-radius: 14px;
        margin-bottom: 20px;
        box-shadow: 0 2px 6px rgba(0,0,0,0.05);
    }

    .ts-row {
        display: flex;
        gap: 20px;
        flex-wrap: wrap;
    }

    .ts-col {
        flex: 1 1 calc(50% - 20px);
        min-width: 260px;
    }

    .ts-check-group {
        display: flex;
        flex-wrap: wrap;
        gap: 16px;
        margin-top: 10px;
    }

    .ts-check-item {
        min-width: 180px;
    }

    .ts-block {
    background: white;
    padding: 20px;
    border-radius: 14px;
    margin-bottom: 24px;
    box-shadow: 0 6px 18px rgba(0,0,0,0.08);
    transition: box-shadow .2s ease, transform .2s ease;
    }
    .ts-block:hover {
        box-shadow: 0 10px 24px rgba(0,0,0,0.12);
        transform: translateY(-2px);
    }

    .ant-form-item {
    margin-bottom: 14px !important;
}

.ant-input, .ant-select-selector, .ant-input-number {
    border-radius: 10px !important;
}

.ts-row {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 20px;
}

.ts-block {
    box-shadow: 0 2px 6px rgba(0,0,0,0.06);
}
.ts-block:hover {
    box-shadow: 0 6px 14px rgba(0,0,0,0.10);
}


.ts-check-item {
    padding: 4px 10px;
    margin-bottom: 10px;
    background: #f5f5f5;
    border-radius: 10px;
    display: flex;
    align-items: center;
    transition: .2s ease;
}
.ts-check-item:hover {
    background: #e9e9e9;
}

.ts-block {
    border: 2px solid #a8a6a0;
}


.ant-input,
.ant-select-selector,
.ant-input-number {
    transition: border-color .15s ease, box-shadow .15s ease;
}

.ant-input:focus,
.ant-select-selector:focus-within,
.ant-input-number:hover {
    box-shadow: 0 0 0 2px rgba(22, 119, 255, 0.2);
}

.ts-container {
    padding: 28px;
    background: #f0f5f2;
    border-radius: 20px;
}

</style>

<div class="ts-container">
    <div class="ts-section-title">Datos Especificos</div>

    <Form Model="Model">


        @* --- INCENDIOS --- *@
        @if (Model is IncendioViewModels incendioView)
        {
             <div class="ts-block">
                <div class="ts-section-title">Datos del Incendio</div>
                <div class="ts-row">
                    <div class="ts-col">
                        <FormItem Label="Causa" Required>
                            <EnumSelect TEnum="TipoCausaIncendio" @bind-Value="@incendioView.SuperficieAfectadaCausa" />
                        </FormItem>
                    </div>
                    <div class="ts-col">
                        <FormItem Label="Dimensiones" Required>
                            <EnumSelect TEnum="TipoSuperficie?" @bind-Value="@incendioView.TipoSuperficieAfectada" />
                        </FormItem>
                    </div>
                </div>

                <FormItem Label="Cantidad">
                    <AntDesign.InputNumber @bind-Value="@incendioView.SuperficieAfectadaIncendio" />
                </FormItem>

                <FormItem Label="Tipo de Evacuación">
                    <EnumSelect TEnum="TipoEvacuacion" @bind-Value="@incendioView.TipoEvacuacion" />
                </FormItem>

                <FormItem Label="Detalle de la superficie afectada" Required>
                    <TextArea Placeholder="Describí qué zonas fueron afectadas y en qué medida" @bind-Value="@incendioView.DetalleSuperficieAfectadaIncendio" />
                </FormItem>

                @if (incendioView is IncendioViviendaViewModels incendioViviendaView)
                {
                        <FormItem Label="Vivienda" Required>
                         <EnumSelect TEnum="TipoIncendioVivienda" @bind-Value="@incendioViviendaView.TipoLugar" />
                        </FormItem>
                }

                @if (incendioView is IncendioAeronavesViewModels incendioAeronavesView)
                {
                    <FormItem Label="Tipo de Aeronave" Required>
                        <EnumSelect TEnum="TipoIncendioAeronaves" @bind-Value="@incendioAeronavesView.TipoAeronave" />
                    </FormItem>
                }

                @if (incendioView is IncendioOtroViewModels incendioOtroView)
                {
                    <FormItem Label="Tipo de Incendio (Otro)" Required>
                        <EnumSelect TEnum="TipoIncendioOtro" @bind-Value="@incendioOtroView.OtroIncendio" />
                    </FormItem>
                }
            </div>
        }

        @* --- MATERIALES PELIGROSOS --- *@
        @if (Model is MaterialPeligrosoViewModels matpelView)
        {
            <div class="ts-block">
                <div class="ts-section-title">Materiales Peligrosos</div>

                <FormItem Label="Tipo de Material Peligroso" Required>
                    <EnumSelect TEnum="CategoriaMaterialPeligroso?" @bind-Value="@matpelView.Tipo" />
                </FormItem>

                <FormItem Label="Sustancias Involucradas" Required>
                    <TextArea @bind-Value="@matpelView.Sustancias" Placeholder="Describa las sustancias involucradas" />
                </FormItem>

                <div class="ts-check-group">
                    <div class="ts-check-item"><Checkbox @bind-Checked="@matpelView.Controlada">Controlada</Checkbox></div>
                    <div class="ts-check-item"><Checkbox @bind-Checked="@matpelView.Venteo">Venteo</Checkbox></div>
                    <div class="ts-check-item"><Checkbox @bind-Checked="@matpelView.DilucionDeVapores">Dilución de Vapores</Checkbox></div>
                    <div class="ts-check-item"><Checkbox @bind-Checked="@matpelView.Neutralizacion">Neutralización</Checkbox></div>
                    <div class="ts-check-item"><Checkbox @bind-Checked="@matpelView.Trasvase">Trasvase</Checkbox></div>
                    <div class="ts-check-item"><Checkbox @bind-Checked="@matpelView.OtraAccionesMateriales">Otra</Checkbox></div>
                </div>

                @if (matpelView.OtraAccionesMateriales)
                {
                    <FormItem Label="Detalles de Otras Acciones (Materiales)">
                        <Input @bind-Value="@matpelView.DetallesAccionesMateriales" Placeholder="Especifique la otra acción" />
                    </FormItem>
                }

                <div class="ts-row">
                    <div class="ts-col">
                        <FormItem Label="Evacuación">
                            <EnumSelect TEnum="TipoEvacuacion?" @bind-Value="@matpelView.Evacuacion" />
                        </FormItem>
                    </div>
                    <div class="ts-col">
                        <div class="ts-check-group">
                            <Checkbox @bind-Checked="@matpelView.Descontaminacion">Descontaminación</Checkbox>
                            <Checkbox @bind-Checked="@matpelView.Confinamiento">Confinamiento</Checkbox>
                            <Checkbox @bind-Checked="@matpelView.SinAccion">Sin Acción</Checkbox>
                            <Checkbox @bind-Checked="@matpelView.OtraAccionesPersonas">Otra</Checkbox>
                        </div>
                    </div>
                </div>

                @if (matpelView.OtraAccionesPersonas)
                {
                    <FormItem Label="Detalles de Otras Acciones (Personas)">
                        <Input @bind-Value="@matpelView.DetallesAccionesPersonas" Placeholder="Especifique la otra acción" />
                    </FormItem>
                }

                <div class="ts-row">
                    <div class="ts-col">
                        <FormItem Label="Tipo de Superficie">
                            <EnumSelect TEnum="TipoSuperficie?" @bind-Value="@matpelView.TipoSuperficie" />
                        </FormItem>
                    </div>
                    <div class="ts-col">
                        <FormItem Label="Cantidad Afectada (m²)">
                            <AntDesign.InputNumber @bind-Value="@matpelView.Cantidad" />
                        </FormItem>
                    </div>
                </div>

                <FormItem Label="Situación de la Explosión">
                    <EnumSelect TEnum="TipoSituacionExplosion?" @bind-Value="@matpelView.TipoSituacion" />
                </FormItem>
            </div>
        }

        @* ACCIDENTES *@
        @if (Model is AccidenteViewModels accidenteView)
        {
            <div class="ts-block">
                <div class="ts-section-title">Accidente</div>

                <FormItem Label="Tipo de Accidente" Required>
                    <EnumSelect TEnum="TipoAccidente?" @bind-Value="@accidenteView.Tipo" />
                </FormItem>

                <FormItem Label="Condiciones Climáticas" Required>
                    <EnumSelect TEnum="TipoCondicionesClimaticas?" @bind-Value="@accidenteView.CondicionesClimaticas" />
                </FormItem>
            </div>
        }

        @* FACTORES CLIMÁTICOS *@
        @if (Model is FactorClimaticoViewModel factorClimaticoView)
        {
            <div class="ts-block">

                <div class="ts-section-title">Factor Climático</div>

                <FormItem Label="Tipo de Factor Climático" Required>
                    <EnumSelect TEnum="TipoFactoresClimaticos" @bind-Value="@factorClimaticoView.Tipo" />
                </FormItem>

                <FormItem Label="Tipo de Evacuación">
                    <EnumSelect TEnum="TipoEvacuacion?" @bind-Value="@factorClimaticoView.Evacuacion" />
                </FormItem>

                <FormItem Label="Tipo de Superficie">
                    <EnumSelect TEnum="TipoSuperficie?" @bind-Value="@factorClimaticoView.Superficie" />
                </FormItem>

                <FormItem Label="Cantidad Afectada (m²)">
                    <AntDesign.InputNumber @bind-Value="@factorClimaticoView.CantidadAfectada" />
                </FormItem>
            </div>
        }

        @* SERVICIOS ESPECIALES *@
        @if (Model is ServicioEspecialViewModel servicioEspecialView)
        {
            <div class="ts-block">
                <div class="ts-section-title">Servicio Especial</div>

                @switch (servicioEspecialView.Tipo)
                {
                    case ServicioEspecialTipo.Representacion:
                        var rep = (ServicioEspecialRepresentacionViewModels)servicioEspecialView;
                        <FormItem Label="Tipo de Representación" Required>
                            <EnumSelect TEnum="TipoServicioRepresentaciones" @bind-Value="@rep.TipoServicioRepresentacion" />
                        </FormItem>

                        @if (rep.TipoServicioRepresentacion == TipoServicioRepresentaciones.Otro)
                        {
                            <FormItem Label="Especifique Otro" Required>
                                <Input @bind-Value="@rep.OtroRepresentacion" />
                            </FormItem>
                        }

                        <FormItem Label="Organización Beneficiada" Required>
                            <EnumSelect TEnum="TipoOrganizacionBeneficiada" @bind-Value="@rep.TipoOrganizacion" />
                        </FormItem>
                        break;

                    case ServicioEspecialTipo.Prevencion:
                        var prev = (ServicioEspecialPrevencionViewModels)servicioEspecialView;
                        <FormItem Label="Tipo de Prevención" Required>
                            <EnumSelect TEnum="TipoServicioPrevencion?" @bind-Value="@prev.TipoPrevencion" />
                        </FormItem>
                        <FormItem Label="Organización Beneficiada" Required>
                            <EnumSelect TEnum="TipoOrganizacionBeneficiada?" @bind-Value="@prev.TipoOrganizacion" />
                        </FormItem>
                        break;

                    case ServicioEspecialTipo.Capacitacion:
                        var cap = (ServicioEspecialCapacitacionViewModel)servicioEspecialView;
                        <FormItem Label="Nivel de Capacitación" Required>
                            <EnumSelect TEnum="TipoNivelCapacitacion" @bind-Value="@cap.NivelDeCapacitacion" />
                        </FormItem>

                        @if (cap.NivelDeCapacitacion == TipoNivelCapacitacion.Otro)
                        {
                            <FormItem Label="Especifique Otro Nivel" Required>
                                <Input @bind-Value="@cap.NivelDeCapacitacionOtro" />
                            </FormItem>
                        }

                        <FormItem Label="Tipo de Capacitación" Required>
                            <EnumSelect TEnum="TipoCapacitacion" @bind-Value="@cap.TipoCapacitacion" />
                        </FormItem>

                        @if (cap.TipoCapacitacion == TipoCapacitacion.Otro)
                        {
                            <FormItem Label="Especifique Otro Tipo" Required>
                                <Input @bind-Value="@cap.TipoCapacitacionOtra" />
                            </FormItem>
                        }

                        <FormItem Label="Día y Hora">
                            <DatePicker Mask="dd/MM/yyyy" @bind-Value="@cap.DiaHora" ShowTime="true" />
                        </FormItem>
                        break;

                    case ServicioEspecialTipo.ColocacionDriza:
                    case ServicioEspecialTipo.SuministroAgua:
                    case ServicioEspecialTipo.FalsaAlarma:
                    case ServicioEspecialTipo.RetiradoDeObito:
                    case ServicioEspecialTipo.ColaboracionFuerzasSeguridad:
                        <p>Formulario en construcción.</p>
                        break;
                }
            </div>
        }
    </Form>
</div>




@code {
    [Parameter, EditorRequired] public SalidasViewModels Model { get; set; } = null!;
    [Parameter, EditorRequired] public bool isUnlocked { get; set; } = false;
    [Parameter, EditorRequired] public EventCallback<bool> OnCompleted { get; set; } // Evento para notificar que se ha completado el formulario
}